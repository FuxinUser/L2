namespace L2
{
    public enum TcpBindStatus
    {
        Success, Fail
    }

    public enum LocalConnectionStatus
    {
        Connected, ConnectionClose, ConnectionFail
    }

    public enum RemoteConnectionStatus
    {
        Connected, ConnectionClose, ConnectionFail
    }
}
